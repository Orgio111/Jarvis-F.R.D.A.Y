"""
Obsidian Vault Integration — Jarvis memory → Markdown knowledge graph.

Modules:
  writer   — low-level vault writer (markdown file ops)
  logger   — high-level structured loggers (agent diary, decisions, memory)
  sync     — real-time sync service (hooks into agent events)
  router   — FastAPI endpoints
"""
